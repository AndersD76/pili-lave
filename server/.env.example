# PILI LAVE — ambiente local (NUNCA commitar este arquivo)

# Neon Postgres — POOLED p/ runtime, DIRETA p/ migrations
DATABASE_URL="postgresql://neondb_owner:SUA_SENHA_NEON@ep-plain-shape-aywvr2xz-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
DIRECT_URL="postgresql://neondb_owner:SUA_SENHA_NEON@ep-plain-shape-aywvr2xz.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Auth
JWT_SECRET="GERE_UM_SEGREDO_LONGO"
ADMIN_PASSWORD="TROQUE_ESTA_SENHA"

# OTP / SMS — console (dev: código sai no log) | brasilsms | twilio
SMS_PROVIDER="console"
SMS_API_KEY=""

# Placa — none (só formato) | apibrasil | placaapi
PLACA_PROVIDER="none"
PLACA_API_KEY=""
PLACA_DEVICE_TOKEN=""

# Asaas — sandbox por padrão; produção: https://api.asaas.com/v3
ASAAS_BASE_URL="https://api-sandbox.asaas.com/v3"
ASAAS_API_KEY=""
# valida webhooks: configure o mesmo valor no painel Asaas (header asaas-access-token)
ASAAS_WEBHOOK_TOKEN="GERE_OUTRO_SEGREDO"
DEVICE_KEY="GERE_UM_SEGREDO"
OTP_MASTER_CODE=""

# Fluxo de reserva
# Cron de expiração (Railway a cada 5 min chama POST /api/cron/expire-reservations
# com o header x-cron-secret). Vazio = aberto (só em teste).
CRON_SECRET=""
# LPR na nuvem p/ câmera dedicada (POST /api/lpr/frame) — token do platerecognizer.com
PLATE_RECOGNIZER_TOKEN=""
# deviceKey gravado na máquina padrão pelo seed (cada ESP32 tem o seu)
MACHINE_DEVICE_KEY=""
